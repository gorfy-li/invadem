package invadem.objects;

import processing.core.PApplet;
import processing.core.PImage;

public class PowerInvader extends Invader {
    //create a subclass of Invader which has similar method to Invader and shoot PowerProjectile
    public PowerInvader(PImage[] img, int x, int y, int width, int height) {super(img, x, y, width, height);}
}
